export default function App() {
  return (
    <main className="app-shell">
      <p className="eyebrow">social-agent</p>
      <h1>Standalone React UI scaffold</h1>
      <p>
        Empty Vite app is ready. The existing CLI, package API, and static demo
        remain unchanged until the React UI is intentionally wired in.
      </p>
    </main>
  );
}
